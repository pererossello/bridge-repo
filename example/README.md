Inverse Crime example

1. Run `make_mock.py` to create a mock catalogue
2. Run `start_rec.py` to start the inference,  `continue_rec.py` to continue it, and `sample.py` to sample parameters. 
3. Run `stats.py` to compute statistics of the samples, and `plot_din_stats.ipynb` to plot summary statistics. 
